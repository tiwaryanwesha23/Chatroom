To install [express](https://www.npmjs.com/package/express) and [socket.io](https://www.npmjs.com/package/socket.io), run the following command:

```shell
npm install express socket.io --save

node server.js
